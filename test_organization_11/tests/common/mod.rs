////////////////// common/mod.rs /////////////////
// Here to simulate a file of helper functions used by multiple test files
// added as a file of a subdirectory so that it's not treated as test file itself
// (NOTE: it's name is `mod.rs` and it's folder is `common`)

pub fn setup() {
    // setup code specific to your library's tests would go here
}

